import 'package:flutter/material.dart';

class HelpAndSupportScreen extends StatelessWidget {
  const HelpAndSupportScreen({super.key});
  static const id = '/helpAndSupportScreen';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
